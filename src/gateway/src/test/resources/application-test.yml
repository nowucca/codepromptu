spring:
  cloud:
    config:
      enabled: false
      fail-fast: false
  config:
    import: ""
  data:
    redis:
      host: localhost
      port: 6379
      database: 1
  profiles:
    active: test

management:
  endpoints:
    web:
      exposure:
        include: health,info,metrics

logging:
  level:
    com.codepromptu: DEBUG
    org.springframework.cloud.gateway: DEBUG
