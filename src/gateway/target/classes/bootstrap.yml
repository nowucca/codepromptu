spring:
  application:
    name: gateway
  cloud:
    config:
      uri: http://config:8888
      username: config
      password: config123
      fail-fast: true
      retry:
        initial-interval: 1000
        max-attempts: 6
        max-interval: 2000
        multiplier: 1.1
